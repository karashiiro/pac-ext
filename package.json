{
  "name": "@goatcorp-pac/pac-ext",
  "private": true,
  "scripts": {
    "daemon:clean": "turbo daemon clean",
    "daemon:start": "turbo daemon start",
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,json,css,md}\""
  },
  "devDependencies": {
    "@goatcorp-pac/tsconfig": "*",
    "prettier": "^3.0.3",
    "tree-sitter-cli": "^0.20.8",
    "turbo": "latest"
  },
  "packageManager": "npm@8.3.1",
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
